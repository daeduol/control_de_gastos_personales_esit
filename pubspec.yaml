name: esit_flutter_control_gastos
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  path: ^1.8.0
  intl: ^0.19.0
  provider: ^6.1.1 # Gestión de estado sencilla y potente

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons:
  flutter_native_splash: ^2.2.0

flutter:
  uses-material-design: true
flutter_icons:
    android: true
    ios: true
    image_path: "assets/icon/icon.png"
flutter_native_splash:
  color: "#ffffff"
  image: assets/icon/icon.png
  android: true
  ios: true
  android_gravity: center
